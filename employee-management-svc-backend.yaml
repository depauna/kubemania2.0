---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: employee-management
    component: "backend"
  name: employee-management-service
  namespace: depauna
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app: employee-management
    component: "backend"
